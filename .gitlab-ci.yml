pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp index.html public/
    - cp style.css public/
    - cp -r src public/
    - cp -r assets public/
  artifacts:
    paths:
      - public
  only:
    - main
