pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp index.html public/
    - cp main.js public/
    - cp style.css public/
    - cp -r scenes public/
    - cp -r assets public/
  artifacts:
    paths:
      - public
  only:
    - main
